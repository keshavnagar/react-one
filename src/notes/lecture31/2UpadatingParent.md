# updating parent state from child and vice versa
1. for updating state of children from parent, we can use concept called lifting the state up.
2. here we lift the state from child to parent than pass the state down using props.
3. for updating state of parent from children, we can pass a function, updates the state from parent to children props, then children update it 

