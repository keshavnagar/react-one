name your css module files with the .module.css or .module.scss extension.
import the css module file in your react component. 
access class names as properties of the imported styled object.
combine multiple class names using template literals or the class liberary
dynamically apply class named based on component state
now we do practical so these rules are better in use 