# so first we do the experiment where it produce the problem 

# first we go to the IncDec file 

<input
        id="input"
        value={step}
        onChange={(e) => handleStep(e)}
        type="number"
      />

in this code if we keep this code, than the code run succesfully, but if we remove the   onChange={(e) => handleStep(e)} than it give error to us and erro as follow

